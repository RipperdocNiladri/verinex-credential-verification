import { ArrowRight, Check, FileUp, GitCompare, ListChecks, Search, ShieldAlert } from 'lucide-react';
import { Link } from 'wouter';
import { PageHeader } from '@/components/app-shell';

const steps = [{ icon: FileUp, number: '01', title: 'Receive the document', body: 'The browser creates a fingerprint and sends a sanitized file receipt to the verification workspace.' }, { icon: Search, number: '02', title: 'Extract the facts', body: 'Credential fields are surfaced so the system can reason about the record, not just its appearance.' }, { icon: GitCompare, number: '03', title: 'Compare signals', body: 'Identity, institution, timeline, and document integrity each contribute visible evidence.' }, { icon: ListChecks, number: '04', title: 'Explain the result', body: 'A Trust Score makes the strength of the available evidence legible, with caveats attached.' }];

export default function HowItWorks() {
  return <div className="mx-auto max-w-6xl px-5 py-8 md:px-10 md:py-12"><PageHeader eyebrow="The workflow" title="From document to decision." description="Four deliberate steps. One reviewer-ready record. No mysterious confidence score." action={<Link href="/verify" className="inline-flex items-center gap-2 rounded-xl bg-[hsl(var(--primary))] px-4 py-3 text-sm font-semibold text-[hsl(var(--primary-foreground))]" data-testid="link-how-start">Run the demo <ArrowRight size={15} /></Link>} /><div className="grid gap-3 md:grid-cols-2">{steps.map(({ icon: Icon, number, title, body }) => <div key={number} className="group rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-7 transition-transform hover:-translate-y-1 md:p-8"><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-2xl bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]"><Icon size={21} /></span><span className="font-mono-ui text-[10px] tracking-[.18em] text-[hsl(var(--muted-foreground))]">{number}</span></div><h2 className="mt-16 text-xl font-semibold">{title}</h2><p className="mt-3 max-w-sm text-sm leading-6 text-[hsl(var(--muted-foreground))]">{body}</p></div>)}</div><section className="mt-16 grid gap-5 md:grid-cols-2"><div className="rounded-2xl border border-[hsl(var(--accent)/.55)] bg-[hsl(var(--accent)/.12)] p-7 md:p-8"><div className="flex items-center gap-2 text-sm font-semibold"><ShieldAlert size={18} className="text-[hsl(28_74%_42%)]" /> Know the boundary</div><p className="mt-5 text-sm leading-6">The current workspace uses DEMO / MOCK institutional records. A “verified” result here demonstrates the product experience; it is not a registrar confirmation.</p></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-7 md:p-8"><p className="font-mono-ui text-[10px] uppercase tracking-[.18em] text-[hsl(var(--muted-foreground))]">Production capabilities</p><ul className="mt-5 space-y-4">{['Registrar and institution registry connections', 'Organization-specific review policies', 'Auditable decisions and access controls'].map((item) => <li key={item} className="flex items-start gap-3 text-sm"><Check size={16} className="mt-0.5 text-[hsl(var(--primary))]" />{item}</li>)}</ul></div></section></div>;
}